///
/// JHybridNitroLunarRangePickerStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridNitroLunarRangePickerStateUpdater.hpp"
#include "views/HybridNitroLunarRangePickerComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::nitrolunarrangepicker::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridNitroLunarRangePickerState>;

std::shared_ptr<const HybridNitroLunarRangePickerProps> JHybridNitroLunarRangePickerStateUpdater::getPropsFromStateWrapper(
    jni::alias_ref<JStateWrapper::javaobject> stateWrapper) {
  if (stateWrapper.get() == nullptr) {
    return nullptr;
  }
  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapper.get();
  if (!stateWrapper->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
    throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapperImpl = jni::alias_ref<react::StateWrapperImpl::javaobject>{
    static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)
  };
  std::shared_ptr<const react::State> state = stateWrapperImpl->cthis()->getState();
  if (state == nullptr) {
    return nullptr;
  }
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridNitroLunarRangePickerState& data = concreteState->getData();
  const std::shared_ptr<const HybridNitroLunarRangePickerProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    throw std::runtime_error("HybridNitroLunarRangePickerState's data doesn't contain any props!");
  }
  return props;
}

void JHybridNitroLunarRangePickerStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridNitroLunarRangePickerSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> newState,
                                           jni::alias_ref<JStateWrapper::javaobject> oldState) {
  std::shared_ptr<JHybridNitroLunarRangePickerSpec> hybridView = javaView->getJHybridNitroLunarRangePickerSpec();
  std::shared_ptr<const HybridNitroLunarRangePickerProps> newProps = getPropsFromStateWrapper(newState);
  std::shared_ptr<const HybridNitroLunarRangePickerProps> oldProps = getPropsFromStateWrapper(oldState);
  if (newProps == nullptr) [[unlikely]] {
    throw std::runtime_error("Current StateWrapper doesn't contain any props!");
  }

  // Update only props that differ from the previous State snapshot.
  if (oldProps == nullptr
        ? newProps->language.isProvided()
        : !newProps->language.hasSameValue(oldProps->language)) {
    hybridView->setLanguage(newProps->language.get());
  }
  if (oldProps == nullptr
        ? newProps->theme.isProvided()
        : !newProps->theme.hasSameValue(oldProps->theme)) {
    hybridView->setTheme(newProps->theme.get());
  }
  if (oldProps == nullptr
        ? newProps->showLunarDate.isProvided()
        : !newProps->showLunarDate.hasSameValue(oldProps->showLunarDate)) {
    hybridView->setShowLunarDate(newProps->showLunarDate.get());
  }
  if (oldProps == nullptr
        ? newProps->firstDayOfWeek.isProvided()
        : !newProps->firstDayOfWeek.hasSameValue(oldProps->firstDayOfWeek)) {
    hybridView->setFirstDayOfWeek(newProps->firstDayOfWeek.get());
  }
  if (oldProps == nullptr
        ? newProps->displayMode.isProvided()
        : !newProps->displayMode.hasSameValue(oldProps->displayMode)) {
    hybridView->setDisplayMode(newProps->displayMode.get());
  }
  if (oldProps == nullptr
        ? newProps->numberOfMonths.isProvided()
        : !newProps->numberOfMonths.hasSameValue(oldProps->numberOfMonths)) {
    hybridView->setNumberOfMonths(newProps->numberOfMonths.get());
  }
  if (oldProps == nullptr
        ? newProps->startDate.isProvided()
        : !newProps->startDate.hasSameValue(oldProps->startDate)) {
    hybridView->setStartDate(newProps->startDate.get());
  }
  if (oldProps == nullptr
        ? newProps->endDate.isProvided()
        : !newProps->endDate.hasSameValue(oldProps->endDate)) {
    hybridView->setEndDate(newProps->endDate.get());
  }
  if (oldProps == nullptr
        ? newProps->minDate.isProvided()
        : !newProps->minDate.hasSameValue(oldProps->minDate)) {
    hybridView->setMinDate(newProps->minDate.get());
  }
  if (oldProps == nullptr
        ? newProps->maxDate.isProvided()
        : !newProps->maxDate.hasSameValue(oldProps->maxDate)) {
    hybridView->setMaxDate(newProps->maxDate.get());
  }
  if (oldProps == nullptr
        ? newProps->closeIconUri.isProvided()
        : !newProps->closeIconUri.hasSameValue(oldProps->closeIconUri)) {
    hybridView->setCloseIconUri(newProps->closeIconUri.get());
  }
  if (oldProps == nullptr
        ? newProps->confirmIconUri.isProvided()
        : !newProps->confirmIconUri.hasSameValue(oldProps->confirmIconUri)) {
    hybridView->setConfirmIconUri(newProps->confirmIconUri.get());
  }
  if (oldProps == nullptr
        ? newProps->onConfirm.isProvided()
        : !newProps->onConfirm.hasSameValue(oldProps->onConfirm)) {
    hybridView->setOnConfirm(newProps->onConfirm.get());
  }
  if (oldProps == nullptr
        ? newProps->onClose.isProvided()
        : !newProps->onClose.hasSameValue(oldProps->onClose)) {
    hybridView->setOnClose(newProps->onClose.get());
  }

  // Update hybridRef if it changed
  if (oldProps == nullptr
        ? newProps->hybridRef.isProvided()
        : !newProps->hybridRef.hasSameValue(oldProps->hybridRef)) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newProps->hybridRef.get();
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
  }
}

} // namespace margelo::nitro::nitrolunarrangepicker::views
