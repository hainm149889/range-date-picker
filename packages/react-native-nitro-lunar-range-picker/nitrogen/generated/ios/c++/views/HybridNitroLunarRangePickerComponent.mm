///
/// HybridNitroLunarRangePickerComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridNitroLunarRangePickerComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridNitroLunarRangePickerSpecSwift.hpp"
#import "NitroLunarRangePicker-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::nitrolunarrangepicker;
using namespace margelo::nitro::nitrolunarrangepicker::views;

/**
 * Represents the React Native View holder for the Nitro "NitroLunarRangePicker" HybridView.
 */
@interface HybridNitroLunarRangePickerComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridNitroLunarRangePickerComponent {
  std::shared_ptr<HybridNitroLunarRangePickerSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridNitroLunarRangePickerComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridNitroLunarRangePickerComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridNitroLunarRangePickerShadowNode::defaultSharedProps();
    std::shared_ptr<HybridNitroLunarRangePickerSpec> hybridView = NitroLunarRangePicker::NitroLunarRangePickerAutolinking::createNitroLunarRangePicker();
    _hybridView = std::dynamic_pointer_cast<HybridNitroLunarRangePickerSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroLunarRangePicker::HybridNitroLunarRangePickerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  NitroLunarRangePicker::HybridNitroLunarRangePickerSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridNitroLunarRangePickerProps>(props);
  const auto* oldViewProps = static_cast<const HybridNitroLunarRangePickerProps*>(oldProps.get());
  NitroLunarRangePicker::HybridNitroLunarRangePickerSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // language: enum
    if (oldViewProps == nullptr
          ? newViewProps.language.isProvided()
          : !newViewProps.language.hasSameValue(oldViewProps->language)) {
      swiftPart.setLanguage(static_cast<int>(newViewProps.language.get()));
    }
    // theme: optional
    if (oldViewProps == nullptr
          ? newViewProps.theme.isProvided()
          : !newViewProps.theme.hasSameValue(oldViewProps->theme)) {
      swiftPart.setTheme(newViewProps.theme.get());
    }
    // showLunarDate: boolean
    if (oldViewProps == nullptr
          ? newViewProps.showLunarDate.isProvided()
          : !newViewProps.showLunarDate.hasSameValue(oldViewProps->showLunarDate)) {
      swiftPart.setShowLunarDate(newViewProps.showLunarDate.get());
    }
    // firstDayOfWeek: optional
    if (oldViewProps == nullptr
          ? newViewProps.firstDayOfWeek.isProvided()
          : !newViewProps.firstDayOfWeek.hasSameValue(oldViewProps->firstDayOfWeek)) {
      swiftPart.setFirstDayOfWeek(newViewProps.firstDayOfWeek.get());
    }
    // displayMode: optional
    if (oldViewProps == nullptr
          ? newViewProps.displayMode.isProvided()
          : !newViewProps.displayMode.hasSameValue(oldViewProps->displayMode)) {
      swiftPart.setDisplayMode(newViewProps.displayMode.get());
    }
    // numberOfMonths: optional
    if (oldViewProps == nullptr
          ? newViewProps.numberOfMonths.isProvided()
          : !newViewProps.numberOfMonths.hasSameValue(oldViewProps->numberOfMonths)) {
      swiftPart.setNumberOfMonths(newViewProps.numberOfMonths.get());
    }
    // startDate: optional
    if (oldViewProps == nullptr
          ? newViewProps.startDate.isProvided()
          : !newViewProps.startDate.hasSameValue(oldViewProps->startDate)) {
      swiftPart.setStartDate(newViewProps.startDate.get());
    }
    // endDate: optional
    if (oldViewProps == nullptr
          ? newViewProps.endDate.isProvided()
          : !newViewProps.endDate.hasSameValue(oldViewProps->endDate)) {
      swiftPart.setEndDate(newViewProps.endDate.get());
    }
    // minDate: optional
    if (oldViewProps == nullptr
          ? newViewProps.minDate.isProvided()
          : !newViewProps.minDate.hasSameValue(oldViewProps->minDate)) {
      swiftPart.setMinDate(newViewProps.minDate.get());
    }
    // maxDate: optional
    if (oldViewProps == nullptr
          ? newViewProps.maxDate.isProvided()
          : !newViewProps.maxDate.hasSameValue(oldViewProps->maxDate)) {
      swiftPart.setMaxDate(newViewProps.maxDate.get());
    }
    // closeIconUri: optional
    if (oldViewProps == nullptr
          ? newViewProps.closeIconUri.isProvided()
          : !newViewProps.closeIconUri.hasSameValue(oldViewProps->closeIconUri)) {
      swiftPart.setCloseIconUri(newViewProps.closeIconUri.get());
    }
    // confirmIconUri: optional
    if (oldViewProps == nullptr
          ? newViewProps.confirmIconUri.isProvided()
          : !newViewProps.confirmIconUri.hasSameValue(oldViewProps->confirmIconUri)) {
      swiftPart.setConfirmIconUri(newViewProps.confirmIconUri.get());
    }
    // onConfirm: function
    if (oldViewProps == nullptr
          ? newViewProps.onConfirm.isProvided()
          : !newViewProps.onConfirm.hasSameValue(oldViewProps->onConfirm)) {
      swiftPart.setOnConfirm(newViewProps.onConfirm.get());
    }
    // onClose: function
    if (oldViewProps == nullptr
          ? newViewProps.onClose.isProvided()
          : !newViewProps.onClose.hasSameValue(oldViewProps->onClose)) {
      swiftPart.setOnClose(newViewProps.onClose.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroLunarRangePicker::NitroLunarRangePickerAutolinking::isNitroLunarRangePickerRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  NitroLunarRangePicker::HybridNitroLunarRangePickerSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
